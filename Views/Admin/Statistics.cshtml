@{
    ViewData["Title"] = "Thống Kê - Admin";
    var monthlyRevenue = ViewBag.MonthlyRevenue ?? new List<dynamic>();
    var topProducts = ViewBag.TopProducts ?? new List<dynamic>();
}

<div class="container-fluid py-4">
    <!-- Header -->
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="h3 d-inline-block">
                <i class="bi bi-graph-up"></i> Thống Kê & Báo Cáo
            </h1>
            <a href="@Url.Action("Dashboard", "Admin")" class="btn btn-outline-secondary float-end">
                <i class="bi bi-arrow-left"></i> Quay Lại
            </a>
        </div>
    </div>

    <div class="row">
        <!-- Monthly Revenue Chart -->
        <div class="col-lg-8 mb-4">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-bar-chart"></i> Doanh Thu Theo Tháng
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Tháng</th>
                                    <th class="text-end">Doanh Thu</th>
                                    <th class="text-end">Đơn Hàng</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in monthlyRevenue)
                                {
                                    <tr>
                                        <td>
                                            <strong>Tháng @item.Month/@item.Year</strong>
                                        </td>
                                        <td class="text-end">
                                            @item.Revenue.ToString("#,##0")đ
                                        </td>
                                        <td class="text-end">
                                            <span class="badge bg-info">@item.Orders</span>
                                        </td>
                                    </tr>
                                }
                                @if (!monthlyRevenue.Any())
                                {
                                    <tr>
                                        <td colspan="3" class="text-center text-muted">Chưa có dữ liệu</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Stats -->
        <div class="col-lg-4 mb-4">
            @{
                decimal totalRevenue = 0;
                int totalOrders = 0;
                foreach (var item in monthlyRevenue)
                {
                    totalRevenue += (decimal)item.Revenue;
                    totalOrders += (int)item.Orders;
                }
            }
            <div class="card shadow-sm border-0 mb-3">
                <div class="card-body">
                    <h6 class="text-muted">Tổng Doanh Thu</h6>
                    <h3 class="text-primary">@totalRevenue.ToString("#,##0")đ</h3>
                </div>
            </div>

            <div class="card shadow-sm border-0 mb-3">
                <div class="card-body">
                    <h6 class="text-muted">Tổng Đơn Hàng</h6>
                    <h3 class="text-info">@totalOrders</h3>
                </div>
            </div>

            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h6 class="text-muted">Đơn Hàng Trung Bình</h6>
                    @{
                        var avgOrder = totalOrders > 0 ? totalRevenue / totalOrders : 0;
                    }
                    <h3 class="text-success">@avgOrder.ToString("#,##0")đ</h3>
                </div>
            </div>
        </div>
    </div>

    <!-- Top Products -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-star"></i> Top 10 Sản Phẩm Bán Chạy
                    </h5>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Thứ Hạng</th>
                                <th>Tên Sản Phẩm</th>
                                <th class="text-center">Số Lượng Bán</th>
                                <th class="text-end">Doanh Thu</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int rank = 1;
                            }
                            @foreach (var product in topProducts)
                            {
                                <tr>
                                    <td>
                                        @if (rank == 1)
                                        {
                                            <span class="badge bg-warning text-dark">🥇 @rank</span>
                                        }
                                        else if (rank == 2)
                                        {
                                            <span class="badge bg-secondary">🥈 @rank</span>
                                        }
                                        else if (rank == 3)
                                        {
                                            <span class="badge bg-danger">🥉 @rank</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-light text-dark">@rank</span>
                                        }
                                    </td>
                                    <td><strong>@product.ProductName</strong></td>
                                    <td class="text-center">
                                        <span class="badge bg-info">@product.TotalQuantity</span>
                                    </td>
                                    <td class="text-end">
                                        @product.TotalRevenue.ToString("#,##0")đ
                                    </td>
                                </tr>
                                rank++;
                            }
                            @if (!topProducts.Any())
                            {
                                <tr>
                                    <td colspan="4" class="text-center text-muted">Chưa có dữ liệu</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Export Section -->
    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Xuất Dữ Liệu</h5>
                </div>
                <div class="card-body">
                    <p class="text-muted mb-0">
                        <i class="bi bi-info-circle"></i>
                        Các tính năng xuất Excel, PDF sẽ được thêm trong phiên bản tiếp theo.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 10px;
    }

    .table-hover tbody tr:hover {
        background-color: #f8f9fa;
    }
</style>
